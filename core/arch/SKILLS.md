# Skills & Tools

Three-layer resolution (highest priority first):

```
$JOYA_MY/agents/<name>/toolkit/  →  agent-private
$JOYA_MY/shared/toolkit/         →  team-shared
toolkit/<type>/                  →  framework default
```

Each skill has a `skill.json` descriptor for discoverability. Details → `guides/TOOLKIT.md`.
