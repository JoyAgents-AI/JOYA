# R1. Protocol over Convention

Written protocol always takes precedence over informal habits or unwritten conventions.

- If current practice conflicts with protocol, follow the protocol.
- To change a protocol rule, use the protocol change process (see R11).
